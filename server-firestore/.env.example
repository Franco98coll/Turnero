# JWT para tokens
JWT_SECRET=change-me

# Para Firebase Admin en Render (opción 1: JSON embebido)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account", ...}

# Opción 2 (recomendada): monta un Secret File y usa GOOGLE_APPLICATION_CREDENTIALS
# GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/firebase-sa.json
